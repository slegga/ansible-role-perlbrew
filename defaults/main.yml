---
# some how I cant manage to change from /opt/perl5 library.
# It looks like perlbrew has hardcoded this.

# defaults file for ansible-perlbrew/
perlbrew_root: /home/stein/perl5/perlbrew

default_perl: perl-5.32.0
perls:
  - "{{ default_perl }}"

perlbrew_notest: False

